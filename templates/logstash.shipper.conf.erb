input {
  http {
    host => "0.0.0.0" # default: 0.0.0.0
    port => 8080 # default: 8080
  }
}

filter{
}

output {
   redis { 
     host => "<%= @redis_host %>" 
     port => "<%= @redis_port %>" 
     data_type => "list" 
     key => "logstash" 
     codec => json
   }
   stdout {codec => json }
}
