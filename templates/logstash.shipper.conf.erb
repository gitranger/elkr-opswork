input {
  http {
    host => "0.0.0.0" 
    port => 8080 
    user => logentry
    password => "awesomeL0G"
    #ssl => true
    #keystore => "/etc/logstash/keystore.jks"
    #keystore_password => "awesomeKEY"
  }
}

filter{
}

output {
   redis { 
     host => "<%= @redis_host %>" 
     port => "<%= @redis_port %>" 
     data_type => "list" 
     key => "logstash" 
     codec => json
   }
   stdout {codec => json }
}
